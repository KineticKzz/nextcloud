# INSTALACION DE NEXTCLOUD CON DOCKER-COMPOSE

- Nos descargamos el archivo docker-compose que hay en éste mismo repositorio y lo metemos en una carpeta.

- Ejecutamos dentro de la carpeta:
        
         docker-compose up -d
